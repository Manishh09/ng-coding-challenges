{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/ngc-shell/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],

  "git": {
    "deploymentEnabled": {
      "main": true,
      "develop": true
    }
  },

  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" != \"main\" ] && [ \"$VERCEL_GIT_COMMIT_REF\" != \"develop\" ]; then echo \"🚫 Deployment skipped for branch $VERCEL_GIT_COMMIT_REF\"; exit 0; fi"
}

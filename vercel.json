{
  "version": 2,
  "buildCommand": "npm run build",
  "outputDirectory": "dist/ngc-shell/browser",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "ignoreCommand": "if [ \"$VERCEL_GIT_COMMIT_REF\" = \"main\" ] || [ \"$VERCEL_GIT_COMMIT_REF\" = \"develop\" ]; then echo \"✅ Deployment allowed for $VERCEL_GIT_COMMIT_REF\"; exit 1; else echo \"🚫 Deployment skipped for branch $VERCEL_GIT_COMMIT_REF\"; exit 0; fi"
}
